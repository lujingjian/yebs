在 实体类里添加个自定义时间接收属性。 @ApiModelProperty(value = "创建时间")
                       @JsonFormat(pattern = "yyyy-MM-dd",timezone = "Asia/Shanghai")//去控层
                       private LocalDateTime createDate;
plus直接去控制层写CRUD；
创建一个全局异常类

职称管理 同上增删改